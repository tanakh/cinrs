# The `clang/test/C` revisions whose answer `cinrs` does not match.
#
# One line per *revision* — one `// RUN:` line of one file — named
# `<dir>/<file>.c` when the file has a single RUN line and
# `<dir>/<file>.c:<n>` when it has more. Guard mode requires every
# revision not listed here to come out as the test asks; see
# `doc/clang-c-tests.md` for the outcome classes and the results.
#
# The corpus is not checked in: `scripts/fetch-testsuites.sh llvm` makes
# a sparse checkout of it, and without it the harness skips itself. This
# file therefore holds names and notes and nothing of LLVM's own.
#
# `!` here is a Clang extension `cinrs` declines rather than
# mistranslates — a later revision's feature in an earlier entry point,
# which that entry point is required to refuse.
#
# A note of the form ``see `<id>`` says this revision fails for the same
# reason as another; the report follows the reference, so a cause that
# answers for five revisions of one file is counted once.
#
# One id per line: a marker, the id, a category tag and a note. A case
# that must fail and does fail is *correct*, so only a plain line
# records something wrong, and its tag says what kind of wrong:
#
#   ID  [bug]              `cinrs` is wrong here — it mistranslates
#                          the case, refuses code it means to support,
#                          or emits Rust that will not compile.
#   ID  [unimplemented]    A feature `cinrs` means to have and has not
#                          got to yet.
#   ID  [not-planned]      Deliberately unsupported, with a located
#                          error rather than a mistranslation. Nothing
#                          to fix.
#   ?ID                    It passes or fails depending on the
#                          toolchain, and is guarded neither way.
#   !ID                    This entry point makes the case invalid, so
#                          refusing it is conforming behaviour and not
#                          an error at all. Guard mode requires the
#                          case to fail to *compile*; one that builds
#                          and runs is a failure. The note may open
#                          with `error: "<substring>"`, which the
#                          diagnostic then has to contain.
#
# `doc/testsuites.md` is where the categories are explained. A failure
# that is not listed here at all counts as a bug until somebody says
# otherwise, and an update writes it in as one.
#
# Regenerate with
#
#     CINRS_CLANG_C_REPORT=1 CINRS_CLANG_C_UPDATE_EXPECTED=1 \
#         cargo test --test clang_c
#
# which keeps every `?` line, every `!` line and every tag as it
# stands. Deleting the file first re-measures the notes — and throws
# every tag away, so everything comes back as `[bug]  classify me:`.

!C11/n1310.c:0                          a `//` comment in a `c89!` block; C99 took them from C++ (N644) and Clang has them everywhere as an extension
!C11/n1310.c:1                          `_Static_assert` in a `c99!` block; write `c11!`
!C11/n1310.c:2                          `__builtin_bit_cast`, a Clang-only builtin; the diagnostic is a plain syntax error rather than a named refusal
!C11/n1310.c:3                          `__builtin_bit_cast`; see `C11/n1310.c:2`
!C11/n1310.c:4                          `__builtin_bit_cast`; see `C11/n1310.c:2`
C11/n1330.c            [not-planned]    wrong line: the recovery from a `_Static_assert` where a declaration is not allowed is one error per mistake now, and the file is read to the end. What is left is a *severity*: `_Static_assert(1.0f, …)` on line 56 needs an integer constant expression, Clang folds a floating one as a documented GNU extension and warns, and cinrs refuses it — the same disagreement as `drs/dr4xx.c:1`. Behind it, the `int a, b;` on line 68 is valid C that Clang reports a missing `;` for while it recovers from the line below
C11/n1518.c:0          [unimplemented]  wrong line: cinrs takes a non-ASCII identifier character by the same rule in every revision, where C11 has Annex D's ranges and C23 has XID_Start/XID_Continue, so the C11-only and C23-only errors land on each other's lines
C11/n1518.c:1          [unimplemented]  see `C11/n1518.c:0`
C23/n2322.c            [not-planned]    the line number of a macro invocation spanning spliced lines: cinrs and Clang disagree about which physical line it is, and the paper leaves it unspecified — Clang's own comment calls its answer a FIXME
!C23/n2350.c:3                          a `//` comment in a `c89!` block; see `C11/n1310.c:0`. The other seven revisions of this file — which are about `offsetof` with a nested member designator — now come out as required
!C23/n2508.c:1                          a label at the end of a compound statement in a `c11!` block; C23 made it legal and `c23!` accepts it
!C23/n2549.c:1                          a binary integer constant in a `c17!` block; C23 added them and `c23!` accepts them (Clang takes them as an extension everywhere)
C23/n2607.c:0          [not-planned]    wrong line: what the paper is about now works — a `const` array's qualifiers are its elements' whichever spelling put them there (6.7.3p9), so `&a` is the `const int (*)[1]` the `_Generic` asks for through a `typedef` too. The one `expected-error` left records a *Clang* bug that the file's own comment calls a FIXME: 6.5.15p6 makes the composite of `const int (*)[1]` and `int (*)[1]` a `const int (*)[1]`, which is what cinrs computes and what the comment says the answer should be, and Clang produces `void *`
C23/n2607.c:1          [not-planned]    see `C23/n2607.c:0`
C23/n2836_n2939.c:0    [unimplemented]  wrong line: `\N{...}` named universal character escapes are not implemented, so the `\` is refused as a stray character on line 61 and the escape the test asks about on line 35 is never reached
C23/n2838.c            [not-planned]    wrong line: the `wb` suffix of a `_BitInt` constant is not a suffix cinrs has — `_BitInt` is 🔴 No in doc/c-status.md — so the bound on line 10 that the test says is "juuuuuust right" is refused, and the too-large one on line 7 never gets its own error
C23/n2900_n3011.c:0    [unimplemented]  wrong line: C23's empty initializer for a *variable length array* (N2900) is refused on line 28, so the compound-literal form the test asks about on line 33 is never reached
C23/n2900_n3011.c:1    [not-planned]    wrong line: this revision is `c17!`, where an empty initializer is a C23 feature cinrs gates on the entry point and Clang takes as an extension; the refusal on line 12 comes before the line the test asks about
C23/n3006.c            [unimplemented]  wrong line: `constexpr auto` is taken now, and what refuses line 50 is the *type* it infers: a `constexpr` object of a structure type is not supported yet, only the arithmetic ones are, exactly as for the array of `C23/n3018.c:0`. The empty enumeration in a compound literal the test asks about, on line 42, is never reached
C23/n3018.c:0          [unimplemented]  wrong line: a `constexpr` object of an array type is not supported yet (only the arithmetic types are), so the `constexpr char[]` on line 18 is refused and the overflowing initializer the test asks about on line 12 is never diagnosed
!C23/n3029.c:1                          an enumerator too wide for `int` in a `c17!` block: C23 widens the enumeration (N3029) and `c23!` does too, where 6.7.2.2p2 makes it a constraint violation in every earlier revision. Clang warns instead, which this RUN line does not promote
C23/n3030.c            [not-planned]    wrong line: the rest of N3030 is answered now — a non-defining declaration with a fixed underlying type only stands alone (lines 51, 52, 62, 65, 68, 73, 90), such an enumeration is *complete* where the type is written while one without is not (lines 38 and 43), and a tentative definition of an enumeration nobody completes is a definition of nothing (line 48). Two lines are left. Line 95, `enum e : _Atomic(short)`, is a *Clang* diagnostic and not a C one: C23 6.7.2.2p5 makes the underlying type the unqualified, non-atomic version of what was written, which is exactly what Clang's own `C23/n3030_1.c` asserts and what cinrs computes — Clang answers the spelling with a default-error warning its other file has to switch off, and GCC accepts it silently. Behind it, line 81 wants `enum forward *` and `int *` to be incompatible while the enumeration is still incomplete, and an enumeration *is* `int` in this crate's type model
C23/n3033.c            [not-planned]    not a compilable translation unit: the RUN line is `-E … | FileCheck`, so the file is a *preprocessor output* test and its expansions (`f(0, a, b, c)` at file scope) are not valid C. The harness takes a FileCheck-only file as an accept case, which is right for every other one of them
!C23/n3037_1.c                          the RUN line passes `-Wno-error=odr`, which asks Clang to downgrade a C23 constraint violation to a warning: the file defines `struct T` twice, once with `_Alignas(double)` on its member and once without, and 6.2.7p1 makes an alignment specifier part of what two definitions of one tag have to share. cinrs has no warnings — a procedural macro cannot raise one — so what is left is the diagnostic the standard asks for; Clang's own comment says the case is undefined behaviour there "until we implement attribute structural compatibility logic". The rest of the file is taken as N3037 asks: the repeated `struct S` and the reordered `enum E` are one type each
!C99/block-scopes.c:0                   a `//` comment in a `c89!` block; see `C11/n1310.c:0`
!C99/block-scopes.c:1                   `_Static_assert` in a `c99!` block: cinrs gates a later revision's feature on the entry point, where Clang takes it as an extension (`-Wc11-extensions`, which this RUN line silences)
C99/n448.c:0           [not-planned]    wrong line: `_Static_assert` in a `c99!` block is refused, which is the same conforming gate as `!C99/n570.c:0`; and the file's last `expected-error` is inside `#if __STDC_VERSION__ >= 202311L`, which this revision does not compile — Clang's `-verify` never sees a directive in a skipped conditional and the harness, which reads them out of the raw text, does. The `c23!` revision of this file passes
!C99/n570.c:0                           `_Static_assert` in a `c99!` block; write `c11!`
C99/n809.c             [not-planned]    wrong line: every `_Imaginary` declaration the file asks about *is* diagnosed, and so is the `_Static_assert` it goes on to use — cinrs gates a later revision's feature on the entry point where Clang takes it as an extension. See `C99/n448.c:0`; the sibling files `n809_2.c` and `n809_3.c` now come out as required
!drs/dr094.c:0                          a `//` comment in a `c89!` block; see `C11/n1310.c:0`
drs/dr0xx.c:0          [not-planned]    wrong line: DR011's per-scope composite type and DR012's `(void)&*p` are both answered as asked now, and what stops this revision is the `_Static_assert` on line 151 — the same conforming gate as `C99/n448.c:0`, where Clang takes a later revision's feature as an extension and cinrs gates it on the entry point
drs/dr0xx.c:2          [not-planned]    see `drs/dr0xx.c:0`, with `c99!` in place of `c89!`
drs/dr0xx.c:3          [not-planned]    wrong line: DR027's `$` in an identifier is taken now, and so are DR047 (an array's element type, as a parameter too), DR088 (a lone `struct S;` inside a block declares a tag of *that* block) and DR032's `int dr032 = (1, 2);`. What is left is DR089 on line 478: `#define DR089 object_like` and then `#define DR089(argument) function_like` is the constraint violation 6.10.3p2 describes, cinrs reports it, and GCC and Clang answer it with a warning that only `-pedantic-errors` promotes — and, unlike the leniencies this crate does take, accepting it would have to *choose* which definition wins. Behind it, `func_type fp;` on line 524 — a function declared through a `typedef` of a function type — is read as an object here, so it is refused and the `++fp` of lines 527 to 530 never gets its own error
drs/dr0xx.c:4          [not-planned]    see `drs/dr0xx.c:3`
drs/dr0xx.c:5          [not-planned]    see `drs/dr0xx.c:3`, with one more: this revision is `c23!`, and DR035's two `expected-error` lines are inside `#if __STDC_VERSION__ < 202311L`, which it does not compile. Clang's `-verify` never sees a directive in a skipped conditional and the harness, which reads them out of the raw text, does — the same as `C99/n448.c:0`
!drs/dr206.c:0                          `_Complex` in a `c89!` block, which is the one entry point that has neither it nor `<complex.h>`; the other four revisions of this file now come out as required
!drs/dr253.c:0                          a designated initializer in a `c89!` block; C99 added them (N494), and the other four revisions of this file — which are about designator lists — now come out as required
drs/dr2xx.c:0          [not-planned]    wrong line: `__LLONG_WIDTH__` is predefined now, so the file's own `#error` on line 97 is gone and the revision reads on to the `_Generic` of DR230 on line 94 — a C11 feature in a `c89!` block, which this entry point is required to refuse and Clang's `-Wno-c11-extensions` takes. Same conforming gate as `C99/n448.c:0`
drs/dr2xx.c:1          [not-planned]    see `drs/dr2xx.c:0`, with `c99!` in place of `c89!`
drs/dr2xx.c:3          [not-planned]    wrong line: DR261's null pointer constants (`char *p = 1 - 1;`, lines 300 and 324), DR263's `__builtin_popcount(0)` in a static assertion (line 345), DR252's `void` argument (line 249) and an array too large to have a size (line 373) are all answered now. What is left is line 290, `enum e2 { ex2 = __INT_MAX__ + (0, 1) }`: a constant expression may not hold a comma operator (6.6p3), *GCC refuses it too* ("enumerator value for 'ex2' is not an integer constant"), and Clang folds it as a documented GNU extension — the same disagreement as `drs/dr4xx.c:1` and `C11/n1330.c`. Behind it, DR250's `#nondirective` inside a macro argument (line 218) is not diagnosed: a directive written between the parentheses of an invocation is taken for ordinary argument tokens here
drs/dr2xx.c:4          [not-planned]    see `drs/dr2xx.c:3`
drs/dr2xx.c:5          [not-planned]    see `drs/dr2xx.c:3`
!drs/dr335.c:0                          `_Bool` in a `c89!` block; C99 added it and Clang takes it as an extension in `-std=c89`
!drs/dr335.c:1                          `_Bool` in a `c89!` block; see `drs/dr335.c:0`
drs/dr3xx.c:0          [not-planned]    see `drs/dr3xx.c:1`
drs/dr3xx.c:1          [not-planned]    wrong line: the file's one `expected-error` is the file-scope VLA of DR320 on line 139, and something earlier is refused first in every revision — a variable length array (line 82) in `c89!`, which does not have them, and a `_Static_assert` (line 104) in `c99!`, which does not either; from `c11!` on the file reaches line 226, where a compound literal of variably modified type at file scope is refused and Clang's own comment calls not diagnosing it a FIXME
drs/dr3xx.c:2          [not-planned]    see `drs/dr3xx.c:1`
drs/dr3xx.c:3          [not-planned]    see `drs/dr3xx.c:1`
drs/dr3xx.c:4          [not-planned]    see `drs/dr3xx.c:1`
!drs/dr411.c:1                          `_Static_assert` in a `c99!` block; write `c11!`
!drs/dr464.c:0                          `_Static_assert` in a `c89!` block; write `c11!`
!drs/dr464.c:1                          `_Static_assert` in a `c99!` block; write `c11!`
!drs/dr466.c:0                          a declaration in a `for` clause in a `c89!` block; C99 added it (N740)
!drs/dr483.c:0                          `_Static_assert` in a `c89!` block; write `c11!`
!drs/dr483.c:1                          `_Static_assert` in a `c99!` block; write `c11!`
!drs/dr494.c:0                          `_Alignof` in a `c89!` block; write `c11!`
!drs/dr494.c:1                          `_Alignof` in a `c99!` block; write `c11!`
drs/dr4xx.c:0          [not-planned]    see `drs/dr4xx.c:1`
drs/dr4xx.c:1          [not-planned]    wrong line: the `_Static_assert` on line 108 takes a compound literal, which is not an integer constant expression — Clang folds it as a documented GNU extension and warns, and cinrs refuses it. Under `c89!` and `c99!` the `_Static_assert` itself is the gate. Either way DR423's `const`-qualified `typeof` assignment, which the test asks about, is never reached
drs/dr4xx.c:2          [not-planned]    see `drs/dr4xx.c:1`
drs/dr4xx.c:3          [not-planned]    see `drs/dr4xx.c:1`
drs/dr4xx.c:4          [not-planned]    see `drs/dr4xx.c:1`
!drs/dr5xx.c:0                          an anonymous struct member in a `c89!` block; write `c11!`
!drs/dr5xx.c:1                          an anonymous struct member in a `c99!` block; write `c11!`
