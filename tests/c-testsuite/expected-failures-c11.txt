# The c-testsuite cases `cinrs` does not pass under `c11!`.
#
# Guard mode — the default, and what `cargo test` runs — skips these and
# requires every other selected case to pass. It also runs them, so that
# one which has started passing is reported and the line can go. See
# `doc/c-testsuite.md` for what the causes are.
#
# One test id per line, with a note after it. An id written `?NNNNN`
# passes or fails depending on the toolchain, and is guarded neither
# way. Regenerate with
#
#     CINRS_CTESTSUITE_REPORT=1 CINRS_CTESTSUITE_UPDATE_EXPECTED=1 \
#         CINRS_CTESTSUITE_STANDARD=c11 cargo test --test c_testsuite

00095   extension: converting a function pointer to `void *`
?00140  variadic function definition; needs Rust 1.99, so it passes there and not before
00152   deviation: `__LINE__` names the .rs line, and `#line` does not redirect it
00170   extension: forward-declared (incomplete) `enum`
00204   unsupported: `va_arg` with a struct type
00206   unsupported: `#pragma push_macro` / `pop_macro`, which are ignored
00207   unsupported: variable length array
00209   extension: forward-declared (incomplete) `enum`
00210   extension: `__attribute__((…))`
00213   extension: statement expression `({ … })`
00214   extension: statement expression `({ … })` and `__builtin_expect`
00216   unsupported: an empty `struct` in an initializer, GCC range designators and flexible array members
00220   missing header: <wchar.h> is not bundled
