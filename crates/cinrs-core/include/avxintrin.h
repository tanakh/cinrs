/* <avxintrin.h> — the AVX intrinsics, which live in <immintrin.h> here.
 *
 * GCC and Clang refuse this header when it is included directly and tell you
 * to include <immintrin.h>; cinrs simply does that for you, since the one file
 * is where the declarations are.
 */
#ifndef _CINRS_AVXINTRIN_H
#define _CINRS_AVXINTRIN_H
#include <immintrin.h>
#endif /* _CINRS_AVXINTRIN_H */
