/* The classic include guard, which the preprocessor recognises so that a
 * second `#include` need not read the file at all. */
#ifndef CINRS_TEST_GUARDED_H
#define CINRS_TEST_GUARDED_H
int guarded;
#endif
